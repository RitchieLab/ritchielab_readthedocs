[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "biofilter"
authors = [{name = "Li", email = "xueqiong.li@pennmedicine.upenn.edu"}]
dynamic = ["version", "description"]
dependencies = [
    "mkdocstrings-python",
]